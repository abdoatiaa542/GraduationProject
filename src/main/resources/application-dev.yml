application:
  datasource:
    url: jdbc:mysql://localhost:3306/Graduation_Project?createDatabaseIfNotExist=true
    username: amer
    password: amer
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    logging:
      level:
        org.hibernate.SQL: debug
        org.hibernate.type.descriptor.sql.BasicBinder: trace
